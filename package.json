{
  "name": "stack_finance",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "export $(cat .env | xargs) && node .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "run-scripts": "node ./src/scripts/populate_users.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "agenda": "^4.1.2",
    "express": "^4.17.1",
    "joi": "^17.4.0",
    "mongodb": "^3.6.6",
    "mongoose": "^5.12.6",
    "nodemailer": "^6.6.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/nodemailer": "^6.4.1",
    "tslint": "^6.1.3",
    "typescript": "^4.2.4"
  }
}
